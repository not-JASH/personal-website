<%- include("../partials/main-index", { page: page }) %>

<div class="main-text app-main-text">
    <h1>Jashua Luna</h1>
    <h2 style="color:orange">Please note this website is under construction.</h2>

    <div id="Introduction">
        <% page.intro.forEach(function (paragraph) { %>
            <p><%= paragraph %></p>
        <% }); %>
    </div>

    <div id="Education">
        <h2>Education</h2>
        <% page.education.forEach(function (entry) { %>
            <h3><%= entry.title %></h3>
            <div class="grid-container app-meta-grid">
                <div class="grid-item app-meta-left">
                    <span><%= entry.organization %></span>
                </div>
                <div class="grid-item app-meta-right">
                    <span><%= entry.dates %></span>
                </div>
            </div>
        <% }); %>
    </div>

    <div id="Work-Experience">
        <h2>Work Experience</h2>
        <% page.work.forEach(function (entry) { %>
            <h3><%= entry.role %></h3>
            <div class="grid-container app-meta-grid">
                <div class="grid-item app-meta-left">
                    <span><%= entry.organization %></span>
                </div>
                <div class="grid-item app-meta-right">
                    <span><%= entry.dates %></span>
                </div>
            </div>
            <p><%= entry.summary %></p>
        <% }); %>
    </div>
</div>
